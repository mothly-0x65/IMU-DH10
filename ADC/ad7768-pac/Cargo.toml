[package]
name = "ad7768-pac"
version = "0.1.0"
edition = "2021"
description = "Peripheral Access Crate for AD7768/AD7768-4 simultaneous sampling ADCs"
license = "MIT OR Apache-2.0"

[features]
default = []
# Enable defmt support for debug printing
defmt = ["dep:defmt"]

[dependencies]
defmt = { version = "0.3", optional = true }